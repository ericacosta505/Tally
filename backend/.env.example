# Export these in your shell or configure them in your hosting platform.
# This application does not load this file automatically.
# Generate a real secret with: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=replace-with-a-long-random-secret
# For local development only, omit SECRET_KEY and export TALLY_DEV=1 instead.
# DATABASE_URL=postgresql://user:password@host:5432/tally
PORT=5000
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
